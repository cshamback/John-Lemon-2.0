using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* PROJECTILE ----
    Movement and instantiation are handled by PlayerController.
- move in a straight line from John Lemon > ProjectileAnchor until something is hit
- check if something is hit -> destory self 
- broadcast damage done to damage recipient
- if it hits nothing, destroy after 3 seconds
*/

public class Projectile : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

}